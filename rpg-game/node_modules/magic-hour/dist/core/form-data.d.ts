import { type ReadStream as NodeReadStream } from "node:fs";
export type UploadFile = FileLike | NodeReadStream;
export declare function isUploadFile(val: any): val is UploadFile;
export declare function isNodeReadStream(val: any): val is NodeReadStream;
/**
 * Defines common File spec compatible with modern browser File, node-fetch, etc.
 */
export interface FileLike extends BlobLike {
    lastModified: number;
    name: string;
}
export declare function isFileLike(val: any): val is FileLike;
/**
 * Defines common Blob spec compatible with modern browser Blob, fetch-blob, etc.
 */
export interface BlobLike {
    type: string;
    size: number;
    slice(start?: number, end?: number, type?: string): BlobLike;
    text(): Promise<string>;
    arrayBuffer(): Promise<ArrayBuffer>;
}
export declare function isBlobLike(val: any): val is BlobLike;
export declare function createForm(data: {
    [key: string]: unknown;
}): any;
