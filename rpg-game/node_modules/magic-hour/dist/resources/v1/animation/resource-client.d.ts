import { types } from "../../../index.js";
import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions } from "../../../core";
import * as requests from "../../../resources/v1/animation/request-types";
export declare class AnimationClient extends CoreResourceClient {
    constructor(client: CoreClient);
    /**
     * Animation
     *
     * Create a Animation video. The estimated frame cost is calculated based on the `fps` and `end_seconds` input.
     *
     * POST /v1/animation
     */
    create(request: requests.CreateRequest, opts?: RequestOptions): ApiPromise<types.PostV1AnimationResponse>;
}
